apiVersion: v1
kind: Service
metadata:
  name: mymavenapp-service
spec:
  selector:
    app: mymavenapp
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30080
